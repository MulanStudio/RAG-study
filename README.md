# 🛢️ 油田服务 RAG 系统

一个专为油田服务领域设计的多模态 RAG（检索增强生成）问答系统。

## 🚀 快速开始

### 1. 准备数据

把组委会提供的数据放到 `data/` 文件夹：

```
data/
├── *.pdf          # PDF 文档（支持表格提取）
├── *.xlsx         # Excel 数据
├── *.csv          # CSV 数据
├── *.docx         # Word 文档
├── *.md           # Markdown 文档
├── *.pptx         # PowerPoint
├── *.png / *.jpg  # 图片（VLM 自动生成描述）
└── subfolders/    # 支持子文件夹
```

### 2. 安装依赖

```bash
pip install -r requirements.txt
```

可选（图片 OCR，提升图片问答覆盖）：

```bash
# macOS
brew install tesseract
python3 -m pip install pytesseract
```

### 3. 配置模型（Azure OpenAI）

```bash
# .env 示例
TEAM_DOMAIN=your-team-name
AZURE_OPENAI_API_KEY=your-key
AZURE_OPENAI_MODEL=gpt-5-chat
AZURE_OPENAI_EMBEDDING_MODEL=text-embedding-3-large
```

### 4. 运行系统

```bash
# 方式 1: 命令行交互
python src/member_e_system/app.py

# 方式 2: 直接提问
python src/member_e_system/app.py --question "SLB 的 2023 年营收是多少？"
```

### 5. 批量回答 Excel

比赛题目 Excel（列：no./question/answer）可直接回写答案：

```bash
python3 src/member_e_system/batch_answer_excel.py --input data/questions.xlsx
```

默认配置在 `config/config.yaml` 的 `batch_answer`，支持断点续跑与定期保存。

---

## 📁 项目结构

```
RAG-study/
├── data/                    # 👈 把数据放这里！
│   ├── documents/
│   └── images/
├── config/
│   └── config.yaml          # 统一配置文件
├── src/
│   ├── member_a_data/       # 成员A: 数据层
│   │   ├── loaders/         # 多模态加载器
│   │   ├── metadata_cleaner.py
│   │   └── chunk_summarizer.py
│   ├── member_b_retrieval/  # 成员B: 检索层
│   │   ├── retrieval/       # 检索核心 + LLM Query 改写
│   │   ├── rrf_fusion.py
│   │   └── hyde_retrieval.py
│   ├── member_c_generation/ # 成员C: 生成层
│   │   └── generation/      # 生成核心 + LLM 答案后处理
│   ├── member_d_evaluation/ # 成员D: 评测层
│   │   └── rag_eval.py      # 语义相似度评测
│   └── member_e_system/     # 成员E: 系统层
│       ├── app.py           # 主应用入口
│       ├── batch_answer_excel.py
│       └── azure_openai_client.py
├── scripts/
│   └── prebuild_index.py    # 预构建向量索引
├── requirements.txt
├── TEAM_GUIDE.md
└── README.md
```

---

## ⚙️ 配置说明

编辑 `config/config.yaml` 来调整系统行为：

```yaml
# 模型配置（Azure OpenAI）
models:
  llm:
    provider: "azure_openai"
    model_name: "gpt-5-chat"
  embedding:
    provider: "azure_openai"
    model_name: "text-embedding-3-large"
  azure_openai:
    team_domain: "your-team-name"
    api_key_env: "AZURE_OPENAI_API_KEY"
    completion_model: "gpt-5-chat"
    completion_model_fallback: "gpt-5-mini"
    embedding_model: "text-embedding-3-large"

# 检索配置
retrieval:
  k_excel: 10      # Excel 数据召回数量
  k_word: 5        # Word 文档召回数量

# 批量问答（Excel）
batch_answer:
  input_path: "data/questions.xlsx"
  output_path: ""  # 留空则覆盖输入文件
  question_col: "question"
  answer_col: "answer"
  resume: true
  save_every: 5
```

---

## 🏗️ 系统架构

```
┌─────────────────────────────────────────────────────────────┐
│                        用户问题                              │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│  🆕 LLM Query 改写 (Query Normalization)                     │
│  处理中文音译、同义词、寒暄语过滤                              │
│  "哈利伯顿营收" → "Halliburton revenue"                      │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│  Query Decomposition (问题分解)                              │
│  复杂问题 → 多个子问题                                        │
└─────────────────────────────────────────────────────────────┘
                              │
         ┌────────────────────┼────────────────────┐
         ▼                    ▼                    ▼
┌─────────────┐      ┌─────────────┐      ┌─────────────┐
│  向量检索    │      │   BM25      │      │   HyDE      │
│  (分组召回)  │      │  关键词     │      │  假设文档    │
└─────────────┘      └─────────────┘      └─────────────┘
         │                    │                    │
         └────────────────────┼────────────────────┘
                              │
                              ▼
                    ┌─────────────────┐
                    │   RRF 融合      │
                    └─────────────────┘
                              │
                              ▼
                    ┌─────────────────┐
                    │  Reranker 精排  │
                    └─────────────────┘
                              │
                              ▼
                    ┌─────────────────┐
                    │  CRAG 自我修正  │
                    └─────────────────┘
                              │
                              ▼
                    ┌─────────────────┐
                    │  LLM 生成回答   │
                    └─────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│  🆕 LLM 答案后处理 (Answer Post-processing)                  │
│  - 核心答案提取（移除寒暄/引用/解释）                          │
│  - 选择题格式校正（确保 "A. xxx" 格式）                        │
└─────────────────────────────────────────────────────────────┘
```

---

## 🚀 比赛优化（300MB 大数据量）

### 预构建索引（推荐！）

比赛前一晚运行，提前构建索引：

```bash
python scripts/prebuild_index.py --data_dir competition_data/
```

比赛当天启动 **秒开**（从缓存加载）！

### 性能配置

| 配置项 | 说明 | 效果 |
|--------|------|------|
| `system.vector_db.persist_dir` | 向量索引缓存目录 | 二次启动 < 5 秒 |
| `system.vector_db.force_rebuild` | 强制重建索引 | 数据变化时设为 true |
| `embedding.provider: "huggingface_local"` | 本地 GPU embedding | 不受 API 限制 |
| `indexing_competition` | 大 chunk 配置 | 减少 40% chunk 数量 |

### 本地 Embedding 模型（无 API 限制）

```yaml
# config/config.yaml
models:
  embedding:
    provider: "huggingface_local"
    model_name: "BAAI/bge-large-en-v1.5"
    device: "cuda"  # 或 "cpu"
```

---

## 🔧 核心优化

### 🆕 LLM 驱动的智能处理（避免 hardcode）

| 优化项 | 说明 | 效果 |
|--------|------|------|
| **LLM Query 改写** | 用 LLM 处理中文音译、同义词、寒暄语 | 通用性强，无需维护同义词表 |
| **LLM 核心答案提取** | 用 LLM 移除寒暄语、引用、解释说明 | 答案更简洁，评分更高 |
| **LLM 选择题格式化** | 用 LLM 确保 "A. xxx" 标准格式 | 选择题格式正确率 100% |
| **语义相似度评测** | 基于 embedding 的相似度评分 | 比关键词匹配更准确 |

### 检索增强

| 优化项 | 说明 | 效果 |
|--------|------|------|
| **元数据清洗** | 标准化字段，自动提取日期/公司名 | 提高实体匹配准确率 |
| **文本块摘要** | LLM 生成精炼摘要增强检索 | 长文本召回率提升 |
| **向量索引持久化** | 首次构建后缓存到磁盘 | 二次启动 < 5 秒 |
| **并行 Embedding** | 多线程批量处理 | 构建速度 3-5x |
| **查询扩展** | LLM 生成替代表述 | 召回率提升 |
| **Query Decomposition** | 复杂问题拆分为子问题 | 提高复杂问题覆盖率 |
| **HyDE** | 生成假设文档后检索 | 提高语义匹配准确度 |
| **RRF 融合** | 多路召回按排名融合 | 综合多种检索优势 |
| **PDF 表格提取** | 表格转结构化文本 | 精准检索表格数据 |
| **Parent-Child 分块** | 小块检索，大块生成 | 检索精准 + 上下文完整 |

### 生成质量

| 优化项 | 说明 | 效果 |
|--------|------|------|
| **简化置信度检查** | 仅在检索分数极低时拒绝 | 避免误拒正确答案 |
| **信任大模型输出** | GPT-4/5 无需繁琐验证 | 答案质量大幅提升 |
| **API 自动重试** | 指数退避重试 | 稳定性提升 |

---

## 👥 团队分工

详见 [TEAM_GUIDE.md](TEAM_GUIDE.md)

| 角色 | 负责模块 |
|------|---------|
| 成员A | 数据加载 (`src/member_a_data/`) |
| 成员B | 检索优化 (`src/member_b_retrieval/`) + **LLM Query 改写** |
| 成员C | Prompt 工程 (`src/member_c_generation/`) + **LLM 答案后处理** |
| 成员D | 评测调优 (`src/member_d_evaluation/`) + **语义相似度评测** |
| 成员E | 系统集成 (`src/member_e_system/`, `config/`) |

---

## 📊 评测

```bash
# 运行评测（语义相似度 + LLM-as-Judge）
python src/member_d_evaluation/rag_eval.py

# 输出示例
🧪 Testing: 水力压裂定义
❓ Question: 水力压裂是什么？
🤖 Answer: 向地层中注入高压液体，使岩石产生裂缝，从而释放被困的油气。
📝 Score: 9.5/10
```

---

## 🐛 常见问题

**Q: 图片没有描述**
```bash
ollama pull llava
```

**Q: 想要图片 OCR**
```bash
brew install tesseract
python3 -m pip install pytesseract
```

**Q: 常识计算题支持？**
不支持。仅回答材料内可检索到的计算结果。

**Q: 中文公司名检索不到（如"哈利伯顿"）**
系统已支持 **LLM Query 改写**，自动将中文音译转为英文（如 `哈利伯顿` → `Halliburton`）。

**Q: 带寒暄语的问题回答不准（如"你好，SLB营收是多少？"）**
系统已支持 **LLM 核心问题提取**，自动过滤无关寒暄，提取核心问题。

**Q: 选择题格式不对**
系统已支持 **LLM 选择题格式化**，确保输出 "A. xxx" 标准格式。

---

## 📝 License

MIT
